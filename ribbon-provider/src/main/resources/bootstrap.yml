server:
  port: 9901
spring:
  kafka:
    bootstrap-servers:
    - 118.24.138.39
  application:
    name: ribbon-provider
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      profile: test
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka
management:
  endpoint:
    refresh:
      enabled: true