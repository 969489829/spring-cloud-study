server:
  port: 9001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka
spring:
  kafka:
    bootstrap-servers:
    - 118.24.138.39:9092
  application:
    name: config-client
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      profile: test
      #name:  读取配置文件名，如果没有配置就读取application名称
management:
  endpoint:
    refresh:
      enabled: true
