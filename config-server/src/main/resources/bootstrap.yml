eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka-server #this is the register center's ip-address-service
spring:
  cloud:
    config:
      server:
        git:
          username: jk-crazy
          password: joker3311
          uri: 